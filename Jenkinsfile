pipeline {
    agent any

    stages {
        stage("Clone repo") {
           
        }

        stage("Maven build") {
           
        }

        stage("Test stage") {
            
        }

        stage("Sonar metrics") {
            
        }

        stage("Deployment stage") {

        }
    }
}
